# THIS FILE WAS GENERATED BY HIGGSFIELD. 
# DO NOT EDIT. 
# IF YOUR WORKFLOW DOESN'T WORK, CREATE AN ISSUE.

name: Deploy Experiments

on: 
  push:
    branches:
      - main

concurrency:
  cancel-in-progress: false
  group: main

jobs:
  deploy:
    name: Deploy Experiments
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Set credentials
      run: |
          pip install higgsfield==0.0.3 --quiet

          echo "SSH_KEY='NOTHING'" >> env
          eval "$(jq -r '["key", "user", "port", "hosts"] as $names | . as $json | $names[] as $name | "export \($name | ascii_upcase | @sh)=\($json[$name] | @sh)"' < <(higgsfield ci get-ssh-details))"          
          
          echo "HOSTS=$HOSTS" >> $GITHUB_ENV
          echo "USER=$USER" >> $GITHUB_ENV
          echo "PORT=$PORT" >> $GITHUB_ENV
    - name: Deploy code
      uses: appleboy/ssh-action@v1.0.0
      env:
        HOSTS: ${{ env.HOSTS }}
        USER: ${{ env.USER }}
        PORT: ${{ env.PORT }}      
      with:
        host: ${{ env.HOSTS }}
        username: ${{ env.USER }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ env.PORT }}
        sync: true 
        script: | 
          mkdir -p ~/higgsfield/
          cd ~/higgsfield
          [ -d ~/higgsfield/moj_ai_projekt ] && \
           (cd ~/higgsfield/moj_ai_projekt && \
           git fetch --all && \
           git reset --hard origin/main && \
           git pull origin main) || git clone git@github.com-moj_ai_projekt:SillyusBark/moj_ai_projekt.git moj_ai_projekt || rm -rf ~/higgsfield/moj_ai_projekt && git clone git@github.com-moj_ai_projekt:SillyusBark/moj_ai_projekt.git moj_ai_projekt
          
          echo "SSH_KEY=NOTHING" > env
          echo WAN_DB_TOKEN="${{ secrets.WAN_DB_TOKEN }}" >> env
          echo HUGGINGFACE_TOKEN="${{ secrets.HUGGINGFACE_TOKEN }}" >> env